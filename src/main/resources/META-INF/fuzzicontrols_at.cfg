# FuzziControls Access Transformer
#
# No access transformers are currently required. The legacy `leftClickCounter` transformer
# was removed when attack input moved to KeyBinding.onTick; GuiScreen keyboard/mouse dispatch
# is handled via reflection in the util package.