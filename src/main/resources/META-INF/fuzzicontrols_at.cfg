# FuzziControls Access Transformer

# Makes Minecraft.leftClickCounter publicly accessible so the controller tick handler
# can reset it to force clickMouse() to fire regardless of crosshair target.
public net.minecraft.client.Minecraft field_71429_W # leftClickCounter

